# QuantumIDE

QuantumIDE — это высокотехнологичная мобильная среда разработки (IDE) нового поколения со встроенным искусственным интеллектом для веб-разработки, работающая нативно на Android и построенная на базе Flutter. Она предоставляет полноценное окружение разработчика: редактор, терминал, изолированную песочницу, интеграцию с Git и интеллектуального ассистента.

---

## Стек технологий

- **Фреймворк**: [Flutter](https://flutter.dev) (интерфейс Material 3, адаптивная разметка)
- **Управление состоянием**: [Riverpod](https://riverpod.dev)
- **Маршрутизация**: [GoRouter](https://pub.dev/packages/go_router)
- **Ядро редактора**: `re_editor` + `re_highlight`
- **Эмулятор терминала**: `xterm.dart` + `flutter_pty`
- **ИИ-ядро**: `google_generative_ai` (Gemini) + кастомные коннекторы к DeepSeek, Groq, OpenRouter и локальным ИИ (Ollama/LM Studio/MLC LLM)
- **Темизация и дизайн**: `flex_color_scheme` + визуальные эффекты матового стекла (glassmorphism)
- **Типографика**: *Outfit* (интерфейс) и *JetBrains Mono* (для кода)

---

## Структура проекта

Ниже приведена структура каталогов исходного кода приложения (`lib/`):

*   **`lib/main.dart`**: Главная точка входа Flutter-приложения.
*   **`lib/app.dart`**: Настройки `MaterialApp`, тем оформления и роутера.
*   **`lib/core/`**: Системное ядро и глобальное состояние:
    *   `models/`: Системные структуры данных (диагностика кода, конфигурация провайдеров ИИ).
    *   `providers/`: Глобальные провайдеры состояния (например, локализация).
    *   `router/`: Настройка путей GoRouter.
    *   `theme/`: Конфигурация тем FlexColorScheme и параметров размытия.
    *   `utils/`: Утилиты, конвертер путей (хост-система <-> песочница).
    *   `services/`: Фоновые сервисы и бизнес-логика:
        *   `ai_service.dart`, `ai_autocomplete_service.dart`, `ai_context_compressor.dart`: Интеграция ИИ, автодополнение с дебаунсом и сжатие контекста.
        *   `ai_agent_orchestrator.dart`, `ai_scenario_executor.dart`: Автономные действия ИИ-агентов и выполнение JSON-сценариев.
        *   `lsp_service.dart`, `lsp_autocomplete_service.dart`, `analysis_service.dart`: Интеграция протокола LSP, фоновый анализатор Dart и автодополнение.
        *   `git_service.dart`, `diff_service.dart`: Управление git-командами и подсветка изменений на полях (gutter).
        *   `runtime_service.dart`, `native_terminal.dart`: Развёртывание PRoot окружения Ubuntu и мост к локальным процессам shell.
        *   `microvm_service.dart`: Аппаратная виртуализация KVM на Android 15+ с fallback на PRoot.
        *   `docker_service.dart`: Оркестрация контейнеров Docker/Podman внутри песочницы.
        *   `ssh_service.dart`: Удалённая разработка через SSH (подключение, передача файлов, удалённые команды).
        *   `mlc_llm_service.dart`: Локальный автокомплит кода через NPU чип устройства без интернета.
        *   `wasm_plugin_runner.dart`: Исполнитель WASM-плагинов в песочнице WebView.
        *   `crdt_sync_service.dart`: Синхронизация файлов через CRDT для совместной разработки.
        *   `marketplace_service.dart`: Магазин плагинов и тем (загрузка, установка, управление).
        *   `package_service.dart`, `pub_package_service.dart`: Управление SDK, APT-пакетами, сборками Gradle и pub.dev API.
        *   `workspace_service.dart`, `project_service.dart`: Управление рабочими областями, проектами и шаблонами сборки.
*   **`lib/features/`**: Функциональные модули приложения (Feature-Driven Architecture):
    *   `ai_assistant/`: Панель чата ИИ в реальном времени, настройки и интеграция серверов MCP.
    *   `editor/`: Редактор кода, менеджер вкладок, панель ошибок, структура кода и панель виртуальных клавиш.
    *   `file_explorer/`: Избранное, дерево файлов, глобальный поиск по проекту и анализатор диска.
    *   `git/`: Панель управления версиями, визуальный просмотр разниц (Diff) и страница разрешения конфликтов.
    *   `github/`: Интеграция с GitHub API.
    *   `collaboration/`: Совместная разработка (Live Share) — сессии, удалённые курсоры, чат.
    *   `plugins/`: Менеджер WASM-плагинов — установка, настройка, включение/выключение.
    *   `home/`: Рабочий стол, создание проектов по шаблонам, панель локальных серверов и настройки IDE.
    *   `preview/`: Веб-превью на базе WebView с просмотром консольных логов и управлением браузером.
    *   `terminal/`: Многовкладочный терминал, панель виртуальных клавиш, сборщик APK и подпись приложений.
    *   `rest_client/`: Клиент REST API для тестирования эндпоинтов.
*   **`lib/models/`**: Общие модели данных (сессии чата, пакеты).
*   **`lib/shared/`**: Общие компоненты UI:
    *   `layout/`: Адаптивный разделитель окон с drag-and-drop (`drag_drop_split_view.dart`) и поддержка Fold-устройств (`foldable_split_manager.dart`).
    *   `widgets/`: Стеклянные контейнеры, хлебные крошки, статус-бар и базовые сетки UI.
*   **`lib/l10n/`**: Файлы локализации (RU/EN/ES/FR/ZH).

---

## Реализованный функционал

- **Менеджер рабочих областей**: Выбор папок через Android Storage Access Framework (SAF), закладки, шаблоны проектов, список недавних файлов.
- **Редактор Quantum Editor**: Виртуализация строк, подсветка синтаксиса для многих языков, автосохранение, вкладки, индикатор изменений в файлах, линтер и кастомная панель быстрых символов.
- **Встроенный терминал и песочница**: Терминал PTY на базе Ubuntu 24.04 PRoot. Автоматически монтирует накопители Android, поддерживает автозапуск, автовосстановление процессов и имеет кастомный `.bashrc`.
- **Поддержка протоколов LSP**: Полноценная интеграция языковых серверов для HTML, CSS, JavaScript и TypeScript. Автодополнение кода, всплывающие подсказки документации, поиск использований символа, переименование и автоматическое форматирование исходников.
- **Совместная разработка (Live Share)**: Возможность создания и подключения к сессиям реального времени внутри локальной сети. Синхронизация правок через CRDT, отображение курсоров и выделений других разработчиков с именными ярлыками, а также встроенный текстовый чат сессии.
- **ИИ-Ассистент (Мультимодельный)**: Встроенный чат с поддержкой Google Gemini, Groq, DeepSeek, OpenRouter и локальных моделей (Ollama/LM Studio). Интеллектуальное автодополнение кода на лету с дебаунсом в 700 мс.
- **ИИ-Агенты**: Автономное выполнение действий — создание файлов, запуск команд, исправление ошибок на основе JSON-сценариев с rollback-поддержкой.
- **Интеграция с Git**: Просмотр измененных файлов, коммиты, визуальный diff-просмотрщик (side-by-side) и графическое разрешение конфликтов слияния.
- **MicroVM (KVM)**: Аппаратная виртуализация Linux на Android 15+ для обхода ограничений PRoot. Автоматический fallback на PRoot на старых устройствах.
- **Docker/Podman**: Запуск и оркестрация контейнеров прямо внутри изолированной песочницы с монтированием Android-хранилищ.
- **SSH-разработка**: Подключение к удалённым серверам, выполнение команд, передача файлов (upload/download), управление соединениями.
- **Локальный автокомплит (MLC LLM)**: Интеграция легковесных моделей через NPU чип устройства для автодополнения без интернета.
- **WASM-плагины**: Загрузка и выполнение плагинов на Rust, Go или C++, скомпилированных в WebAssembly в изолированной песочнице.
- **Магазин плагинов и тем**: Установка расширений и визуальных стилей из удалённого реестра.
- **Инструменты сборки и диагностики для Android**: Автоматическая настройка NDK/SDK под ARM64 Linux, сборка APK, подпись ключами, интеграция с JDK 17, запуск `dart analyze` в фоновом режиме, панель отображения системных ошибок.
- **Браузерное превью**: Встроенный WebView с инспектором консоли и мониторингом запущенных локальных веб-серверов.
- **Адаптивный интерфейс**: Drag-and-drop разделение окон, поддержка складных устройств (Fold), ресайз панелей через перетаскивание разделителей.
- **Премиальный UI/UX**: Эффекты Glassmorphism (размытие BackdropFilter), плавные переходы и пружинные анимации, выбор акцентных цветов проекта, шрифты Outfit и JetBrains Mono, кастомные иконки Lucide.
- **Локализация**: Мультиязычный интерфейс (английский, русский, испанский, французский и китайский) с удобным диалогом переключения прямо в настройках приложения.

---

## Дорожная карта (План развития)

Все запланированные модули реализованы. Дальнейшее развитие:

- [ ] Оптимизация производительности MicroVM на различных устройствах
- [ ] Расширение каталога плагинов сообществом
- [ ] Улучшение AI-агентов: поддержка многошаговых сценариев с ветвлениями
- [ ] Тестирование на折叠ных устройствах (Samsung Galaxy Fold, Pixel Fold)
- [ ] Интеграция дополнительных языковых серверов (Python, Rust, Go)

---

## Присоединяйтесь к разработке!

Проект полностью открыт для сообщества, и мы очень ждем новых разработчиков, тестировщиков и дизайнеров! Если вы хотите исправить баг, добавить новую фичу, улучшить документацию или протестировать IDE на своем устройстве — добро пожаловать.

### Как помочь проекту:
1. **Сделайте форк** репозитория и клонируйте его к себе.
2. Изучите дорожную карту выше или найдите интересную задачу в разделе Issues на GitHub.
3. Настройте локальную среду разработки:
   ```bash
   flutter pub get
   flutter run
   ```
4. Создайте ветку для вашей фичи, сделайте коммит изменений и отправьте **Pull Request**.

Если у вас возникли вопросы по архитектуре или вы хотите обсудить идеи, создавайте issue или пишите в обсуждения!

---

## Лицензия

Этот проект распространяется под лицензией MIT — подробности можно найти в файле `LICENSE`.
